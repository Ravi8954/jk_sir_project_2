on:
  push:
jobs:
  my_first_job:
    name: first job
    continue-on-error: true
    runs-on: ubuntu-slim
    steps:
      - run: echo "my first job ran successful"
  my_second_job:
    name: second job
    needs: my_first_job
    runs-on: ubuntu-slim
    steps:
      - run: echo "my second job job ran successful"
