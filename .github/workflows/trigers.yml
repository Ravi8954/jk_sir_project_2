name: githubtrigeraction
on:
  # workflow_dispatch:
  push:
    branches:
      - main
      - feature
      - develop

jobs:
  first_job:
    name: first job
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello world"
